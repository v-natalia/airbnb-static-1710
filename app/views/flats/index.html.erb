<div class="container">

  <h1>Flats</h1>
  <% @flats.each do |flat| %>
    <h2 class="mt-3">
      <%= link_to flat["name"], flat_path(flat["id"])  %>
    </h2>
    <p>
      <%= flat["price"] %> <%= flat["priceCurrency"] %>
    </p>
  <% end %>
</div>
